apiVersion: v1
kind: Pod
metadata:
  name: nginx-3
spec:
  containers:
  - name: nginx-3
    image: nginx
    resources:
      limits:
        memory: "2Gi"
        cpu: "2000m"
      requests:
        memory: "2Gi"
        cpu: "2000m"
